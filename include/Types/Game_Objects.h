#pragma once

/// \brief Enum representing the types of objects in the game world.
enum class GameObjects {
    Coin = 0,
    Enemy,
    Ladder,
    Player,
    Rope,
    Wall,
    DiggableFloor,
    Max
};
