#pragma once

/// \brief Enum representing the types of sounds/effects.
enum class Sounds {
    FinishLevel,
    LevelComplete,
    GameOver,
    GotCoin,
    LifeLost,
    Falling,
    MarioBackground,
    Max
};